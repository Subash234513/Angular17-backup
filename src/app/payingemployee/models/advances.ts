export interface Advances {

        id: string;
        code: string,
        name: string,
        fromDate: string,
        toDate: string,
        advStatus: string,
        history: string,
        edit: string,
        delete: string  
    
}
